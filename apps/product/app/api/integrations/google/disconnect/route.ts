import {NextResponse} from "next/server";import {db} from "@atlas/db";import {createGoogleRuntime} from "@atlas/integrations-google";import {AuditRepository} from "@atlas/repositories";import {optionalAtlasPrincipal} from "@/lib/session";import {canManageIntegrations} from "@/lib/permissions";
export const runtime="nodejs";
export async function POST(request:Request){const principal=await optionalAtlasPrincipal();if(!principal)return NextResponse.json({message:"Authentication required."},{status:401});if(!canManageIntegrations(principal))return NextResponse.json({message:"Admin role is required."},{status:403});const scope={tenantId:principal.tenantId,workspaceId:principal.workspaceId};try{const result=await createGoogleRuntime(db()).tokens.disconnect(scope);await new AuditRepository(db()).record(scope,{actorId:principal.userId,action:"integration.disconnected",targetType:"integration_connection",targetId:result.connection.id,metadata:{integrationId:"google-workspace",providerRevoked:result.revoked,surface:"product-next"}});return NextResponse.redirect(new URL("/app/integrations",request.url),303)}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"Google disconnect failed."},{status:503})}}
