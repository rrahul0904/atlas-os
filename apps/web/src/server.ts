import{createServer}from"node:http";import{readFile}from"node:fs/promises";import{fileURLToPath}from"node:url";import{dirname,resolve}from"node:path";import{renderDemo,renderIndex,type DemoId}from"./render.js";
const root=resolve(dirname(fileURLToPath(import.meta.url)),"../../..");const port=Number(process.env.PORT||3000);const valid=new Set<DemoId>(["founder","ceo","dental","contractor","agency"]);
createServer(async(req,res)=>{const path=(req.url||"/").split("?")[0];if(path==="/health"){res.writeHead(200,{"content-type":"application/json"});res.end(JSON.stringify({status:"ok",product:"AtlasOS"}));return}if(path==="/assets/atlas.css"){const css=await readFile(resolve(root,"apps/web/static/atlas.css"),"utf8");res.writeHead(200,{"content-type":"text/css; charset=utf-8"});res.end(css);return}if(path==="/"){res.writeHead(200,{"content-type":"text/html; charset=utf-8"});res.end(renderIndex());return}const match=path.match(/^\/demo\/(founder|ceo|dental|contractor|agency)$/);if(match&&valid.has(match[1] as DemoId)){res.writeHead(200,{"content-type":"text/html; charset=utf-8"});res.end(renderDemo(match[1] as DemoId));return}res.writeHead(404,{"content-type":"text/plain"});res.end("Not found")}).listen(port,()=>console.log(`AtlasOS web listening on :${port}`));
