import {NextResponse} from "next/server";import {db} from "@atlas/db";import {createGoogleRuntime} from "@atlas/integrations-google";import {optionalAtlasPrincipal} from "@/lib/session";import {canManageIntegrations} from "@/lib/permissions";
export const runtime="nodejs";
export async function GET(request:Request){const principal=await optionalAtlasPrincipal();if(!principal)return NextResponse.redirect(new URL("/login",request.url),303);if(!canManageIntegrations(principal))return NextResponse.json({message:"Admin role is required."},{status:403});const url=new URL(request.url),providerError=url.searchParams.get("error");if(providerError)return NextResponse.json({message:providerError.slice(0,200)},{status:400});try{await createGoogleRuntime(db()).oauth.complete(principal,url.searchParams.get("state")||"",url.searchParams.get("code")||"");return NextResponse.redirect(new URL("/app/integrations",request.url),303)}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"Google OAuth callback failed."},{status:400})}}
