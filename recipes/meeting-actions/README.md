# meeting-actions

This AtlasOS recipe is sourced from the repository recorded in `metadata.json`. During consolidation the original n8n workflow remains authoritative on its preserved `source/automation-*` branch. Native AtlasOS migration must preserve the trigger/condition/action behavior while routing external writes through the shared Agent Governance layer.
